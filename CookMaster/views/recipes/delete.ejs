<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Cookmaster - Deletar Receita</title>
</head>

<body>
  <header>
    <%- include("../header.ejs") %>
  </header>
  <h1>Cookmaster - Deletar Receita</h1>
  <% if (message) {%>
  <div class="message">
    <%= message %>
  </div>
  <% } %>
  <div>
    <form action="/recipes/<%= recipe.id %>/delete" method="POST">
      <label for="passwordInput">Senha: </label>
      <input type="password" name="password" id="passwordInput" /><br />
      <button type="submit">Apagar receita</button>
    </form>
  </div>
  <button onclick="window.location.pathname='/'">Ir para Home</button>
  <button onclick="javascript:history.back()">Voltar</button>

</body>

</html>